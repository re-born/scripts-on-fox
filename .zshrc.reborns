autoload -Uz add-zsh-hook
add-zsh-hook precmd notify-apt-get
notify-apt-get() {
  last_command=$history[$(($HISTCMD-1))]
  if echo $last_command | grep -q apt-get; then
    /usr/bin/ruby1.9.3 /usr/local/lib/reborns_script/notify_installing_command.rb $last_command
  fi
}
